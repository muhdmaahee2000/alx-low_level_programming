#include <stdio.h>
#include "main.h"

/**
 * _abs - This function computes absolute value of an integer.
 *
 * @r: r r is an acsii character
 *
 * Return: Always 0.
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
